# v3.9.2 管理端穩定重建版

請先閱讀根目錄 `00_v3.9.2_穩定重建版_請先看.txt`。

本版固定使用 v3.9.2 版本號，管理端主選單只保留：會員列表、新增會員、客訂商品、統計報表、店員管理、系統設定。
會員詳細頁內保留新增消費、消費紀錄、客訂、會員資料與點數紀錄。

管理端主要讀取改為伺服器端直接存取 Supabase 資料表，降低因資料庫 RPC 版本差異造成整頁無法讀取的風險。新增消費 / 作廢 / 編輯交易品項仍使用既有 `admin_write_v2`，以維持點數與交易的原子一致性。
